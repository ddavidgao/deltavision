name: codeql

# Free security analysis from GitHub. Reports CVE-class issues in the
# Security tab. Scheduled weekly + on main pushes. Free for public repos.
# Docs: https://docs.github.com/en/code-security/code-scanning

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 0 * * 0'   # Sunday midnight UTC

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze Python
    # Code scanning / SARIF upload requires GitHub Advanced Security on
    # private repos (paid feature on personal accounts). It's free on public
    # repos. Skip analysis on the private source-of-truth repo — results
    # upload from the public mirror covers the same code either way.
    if: github.repository == 'ddavidgao/deltavision'
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
      - uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: python
          queries: security-and-quality

      - name: Autobuild
        uses: github/codeql-action/autobuild@v4

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:python"
